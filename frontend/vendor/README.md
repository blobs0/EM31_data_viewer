Place offline copies of `leaflet.js` and `leaflet.css` here (for example from https://unpkg.com/leaflet@1.9.4/dist/).  
The frontend will fall back to a simple canvas view if these files are missing.
